/target
# Per-developer build env (signing identities, SDK paths). cargo truce
# reads this file for [env] values - keep it out of the repo.
/.cargo/config.toml

/vendor
/crates/chord-detection/src/chord_test.rs
